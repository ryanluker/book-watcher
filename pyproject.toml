[project]
name = "book-watcher"
version = "0.1.0"
description = "Automated playwright script to watch books on ORL / Kobo"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "playwright>=1.60.0",
]
